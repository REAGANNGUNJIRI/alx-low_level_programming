#include "main.h"


/**
 * _I SLOWER
 * itchecks for lower case character
 * return 1 for lower case character and 0 for anything else
 */

int _islower(int c)
{
	if (c>= 97 && c <=122)
	{
	
	return (1);
	}
	return (0);
}
