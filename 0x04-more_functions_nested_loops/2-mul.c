#include "main.h"

/**
 * mul - multiplies two intergers
 * @x: the first number
 * @y: the second number
 * Return: the result of multiplication of x and y
 */

int mul(int x, int y)

{
	return (x * y);
}
